
const defaultState = {
    items: [],
    isFetching: true
}

export const reposReducer = (state = defaultState, action) => {
    switch (action.type) {

        default:
            return state
    }
}